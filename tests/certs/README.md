# Test Certifications

This directory stores suite-scoped test certifications produced by:

```bash
cargo xtask test <suite>
```

Files are JSON and are used by local hooks as cache/documentation artifacts.
Details are in `docs/test-certification.md`.
